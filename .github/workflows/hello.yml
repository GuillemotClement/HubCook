name: Hello

on:
  push:
    branches:
      -main
  pull_request:
    branches: 
      -main

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello to everyone
        run: |
          echo "Hello"
          echo "Goodbye"
